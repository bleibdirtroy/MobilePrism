import 'package:flutter_test/flutter_test.dart';
// import 'package:mobileprism/services/controller/data_controller.dart';

void main() {
  group("description", () {
    test("description", () async {
      // final dataController = DataController();
      // final yearsAndMonths = await dataController.getOccupiedDates();
      // print(yearsAndMonths.length);
      // for (final year in yearsAndMonths.keys) {
        // print(year);
        // print(yearsAndMonths[year]!.length);
        // for (final month in yearsAndMonths[year]!) {
        //   // print(month);
        // }
      // }
    });
  });
}
